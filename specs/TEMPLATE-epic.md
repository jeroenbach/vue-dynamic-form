---
id: EPIC-000
type: epic
status: draft
created: YYYY-MM-DD
approved_by: ""
---

# Epic: <name>

## Vision & goal
The initiative as a whole: the larger problem that spans multiple features, who it is for, and what success looks like at the epic level (not per feature).

## Scope
### In scope
The themes/capabilities this epic covers.
### Out of scope (explicit)
What this epic deliberately does not attempt, so features are not smuggled in later.

## Feature breakdown
Filled by product-owner. The candidate features this epic decomposes into: for each, a working title, a one-line scope, and rough delivery order plus dependencies between them. These are proposals, not commitments; each becomes its own `FEAT-XXX` (carrying `epic: EPIC-XXX`) via `/spec:feature` AFTER the epic is approved. No design or architecture happens here; that lives at feature level.

## Constraints & assumptions
Cross-cutting constraints that apply to the whole initiative (public API compatibility, semver posture, sequencing, external deadlines, etc.).

## Open questions
Questions for Jeroen. Must be resolved before approval.

## Features
Filled as features are elaborated. Links to the `FEAT-XXX` folders that carry `epic: EPIC-XXX`, with a one-line status rollup (which are done, in progress, not yet started). This is the live index of the epic; the Feature breakdown above is the original plan.
