---
id: ST-02
type: story
feature: FEAT-002
status: approved
created: 2026-09-17
approved_by: Jeroen
pr: ""
---

# Story: Ephemeral `insertionOrder` and the `displayOrder` render-order channel

## Functional
### User story
As a template author rendering a repeatable explicit choice, I want an opt-in, instance-local record of the order in which occurrences were added (across branches), and a static per-choice flag that makes the engine actually render occurrences in that add-order instead of grouped-by-branch order, so that I can build an interleaved display without writing anything into form `values`.

### Acceptance criteria

1. **Default is byte-identical to today (flag absent/`'grouped'`).**
   Given a repeatable explicit choice with `displayOrder` absent or `'grouped'`,
   When occurrences are added, removed, and re-rendered,
   Then the engine's `renderedChoiceOccurrences` (the new internal render-list computed) is identical, in both membership and order, to `activeChoiceOccurrences`; DOM order and every `globalIndex` value are byte-identical to ST-01's behaviour.

2. **`insertionOrder` is assigned at add-press time, in press order, across branches.**
   Given `addChoiceOccurrence('crmExport')`, then `addChoiceOccurrence('apiEndpoint')`, then `addChoiceOccurrence('crmExport')` again,
   When each occurrence's `-choice-array-item` slot is read,
   Then the three occurrences report `insertionOrder` `1`, `2`, `3` respectively, matching click order, independent of their grouped (branch-then-index) position.

3. **`insertionOrder` is `undefined` for occurrences never added this session.**
   Given a choice mounted with pre-existing (loaded) occurrences already in the value tree,
   When their `-choice-array-item` slots render, with no add-press having occurred yet this session,
   Then their `insertionOrder` is `undefined`.

4. **`insertionOrder` never reaches form `values`.**
   Given any mix of added occurrences,
   When the raw submitted `values` object is inspected (e.g. at `handleSubmit`),
   Then no `insertionOrder`-shaped key appears anywhere in it; the counter lives only in an instance-local ref, exposed solely as slot-prop data.

5. **`displayOrder: 'added'` sorts the render list with a deterministic two-key comparator (AR-3).**
   Given a choice with `displayOrder: 'added'`, mounted with pre-existing occurrences (no `insertionOrder`) and then one new occurrence added this session,
   When `renderedChoiceOccurrences` is computed,
   Then occurrences with no `insertionOrder` come first, ordered by their grouped (branch-then-index) position, followed by occurrences that do have an `insertionOrder`, ordered ascending by that value; no `NaN`/implementation-defined ordering ever occurs for the mixed case.

6. **`globalIndex` reflects the displayed sequence, not the grouped one, when interleaved.**
   Given `displayOrder: 'added'` and the mixed set from AC5,
   When each occurrence's `-choice-array-item` slot renders,
   Then `globalIndex` is that occurrence's position within `renderedChoiceOccurrences` (the sequence actually shown), so `globalIndex + 1` still reads a clean 1..N of what the reader sees, per design decision 2.

7. **Interleaved reorder preserves component/field state (no remount).**
   Given occurrences already rendered in grouped order with some field data entered,
   When an add-press changes the sort so `renderedChoiceOccurrences`'s order changes,
   Then existing occurrences' `DynamicFormItem` instances are reused (same Vue keyed `v-for` instance, since keys are the stable `occurrenceKey`, unaffected by display order), preserving entered field values, touched/validation state, and focus, not remounting them.

   PROPOSED (adversarial review, finding 2): reword the When clause. Under the decided AR-3 append-only stable comparator, a newly-added occurrence always appends at the end of the numbered group and never demotes an already-rendered occurrence's position, and the missing-`insertionOrder` group's relative order never changes on an add either, so "an add-press changes the sort so order changes" for an already-rendered occurrence is unreachable within a session (verified against the comparator: every add is an append to the displayed sequence). Replace with: "When an add or remove changes `renderedChoiceOccurrences`'s composition (an add appends a new entry; a remove drops one and shifts the rest)". This makes the AC describe the actually-reachable, load-bearing property (a keyed `v-for` composition change is remount-free) that the QA plan already tests, rather than a scenario that cannot occur.

8. **`displayOrder` is static, read once at setup.**
   Given `displayOrder` is set in the initial metadata,
   When a `computedProps` function attempts to mutate `thisField.displayOrder` at runtime,
   Then the change has no effect on rendering (the property is excluded from `ComputedPropsFieldType`, mirroring `explicitChoiceSelection`/`preserveOnSwitch`/the sibling static flags), so the render mode cannot flip mid-form.

9. **Removal renumbers `globalIndex` live in interleaved mode too.**
   Given `displayOrder: 'added'` with 4 displayed occurrences,
   When one is removed,
   Then the 3 survivors' `globalIndex` re-derive to 0, 1, 2 within the remaining displayed sequence, extending ST-01 AC2 to the sorted list.

10. **No effect on `maxOccurs: 1` explicit choices.**
    Given a `maxOccurs: 1` explicit choice with `displayOrder` set (a no-op location for this flag),
    When it renders,
    Then behaviour is unchanged from FEAT-001/ST-05 (this story's scope is `maxOccurs > 1` only; `insertionOrder`/`displayOrder` have no observable effect there).

### Edge cases
- All occurrences loaded, none added this session, `displayOrder: 'added'`: every occurrence has `insertionOrder === undefined`, so the two-key comparator's "missing group" covers everyone and the result equals grouped order exactly (documented fallback made concrete).
- An occurrence added, then removed, then a different one added: the `insertionCounter` does not reuse the removed occurrence's number; it keeps incrementing (no compaction in this ephemeral tier, unlike ST-03's persisted `order`).
- A repeatable explicit choice nested inside a reindexing array: `insertionOrder` keys off the stable `occurrenceKey` (unchanged by ancestor reindex), so per-instance isolation holds the same way ST-01 AC6 holds for `globalIndex`.

### Out of scope
- Any persisted `order` value in `values`, mount-time backfill, or compaction on removal: ST-03.
- Any `DynamicFormSettings` change: explicitly rejected (Q8, decided Jeroen 2026-09-17); this story ships no settings surface.
- Any docs/example UI exercising `displayOrder` or a visible toolbar: ST-05.
- A reactive/runtime toggle for `displayOrder`: it is static by design (ADR-5); any UI that flips it does so by remounting the form, which is ST-05's concern, not this story's.

## Design reference
Feature prototype (`../../prototype.html`): `#ordering-interleaved` (the ephemeral tier's visual outcome: DOM order equals visual order per design decision 5, ADR-4; the amber reload-fallback caveat callout), `#reload-behavior` (the ephemeral half of the side-by-side: instance-local counter, gone on remount, falls back to grouped order). This story delivers the engine mechanism only; the toolbar control that lets a reader flip `displayOrder` live is docs-demo chrome built in ST-05, not shipped here.

## Architecture reference
Builds directly on ST-01's `globalIndex` (this story makes the list `globalIndex` loops over sortable) and on FEAT-001/ST-05's static-flag-plus-`ComputedPropsFieldType`-exclusion pattern (`explicitChoiceSelection`, `preserveOnSwitch`), against the feature architecture's Slice B, ADR-2, ADR-3 (render-loop reorder is engine-side, ADR-4), ADR-5, and the "deterministic two-key sort" resolution of adversarial finding 3.

- **`packages/core/src/types/FieldMetadata.ts` (modified).** Add `displayOrder?: 'grouped' | 'added'` (default `'grouped'`) next to `preserveOnSwitch`; add it to the `ComputedPropsFieldType` `Omit` list for the same reason as its siblings (a `computedProps` mutation must not flip render order mid-form).
- **`packages/core/src/components/DynamicFormItemChoice.vue` (modified).**
  - Static capture: `const displayOrder = props.fieldMetadata?.displayOrder === 'added'`, mirroring the existing `explicitChoiceSelection`/`preserveOnSwitch` captures.
  - New instance-local refs: `insertionOrders = ref<Map<string, number>>(new Map())` (keyed by `occurrenceKey`) and a plain (non-reactive, analytics-style) `insertionCounter` variable, incremented on each add-press.
  - In `addChoiceOccurrence`'s `maxOccurs > 1` branch, immediately after `branchFieldArrays[index]?.push(...)`, resolve the just-created occurrence's `occurrenceKey` (branch length before push, per ADR-2: assignment happens at add-press time, not lazily while iterating the grouped list) and call `insertionOrders.value.set(occurrenceKey, ++insertionCounter)`.
  - New computed `renderedChoiceOccurrences`, deterministic two-key sort: equal to `activeChoiceOccurrences` when `displayOrder` is falsy; otherwise a stable sort where occurrences with no `insertionOrders` entry sort first (in their original grouped/`activeChoiceOccurrences` order) followed by occurrences that do have one, ascending by that value.
    - PROPOSED (adversarial review, finding 3): add a `_analytics_renderedChoiceOccurrencesCalculatedCount` plain-variable counter incremented at the top of this computed, mirroring the existing `_analytics_activeChoiceOccurrencesCalculatedCount`/`_analytics_occurrencesCalculatedCount` counters (`DynamicFormItemChoice.vue:50-51,181,277`). This story adds a genuinely new reactive `computed` (unlike ST-01, which added zero new computeds and for which an indirect DOM-identity proof was sufficient), so the direct recompute-count guard the component gives every other computed is the right, cheaper-to-maintain render-discipline test here. Wire it into the `.analytics.test.ts` plan item 5 below (making that test unconditional rather than placeholder).
  - The `-choice-array-item` `v-for` (the `explicitChoiceSelection && maxOccurs > 1` template branch) switches its source list from `activeChoiceOccurrences` to `renderedChoiceOccurrences`; `globalIndex` (ST-01) becomes the loop index over this now-possibly-sorted list, satisfying AC6 without any change to ST-01's own binding.
  - `activeChoiceOccurrences` itself, the choice-level slot prop, is untouched (per the feature's hard constraint and finding 5).
- **`packages/core/src/components/DynamicFormTemplate.vue` (modified, types only).** Add `insertionOrder?: number` to `ChoiceArrayItemAttributes`. `ChoiceOccurrence` stays unchanged (DECIDED research, finding 5: it is dropped from that type entirely, since `activeChoiceOccurrences` never carries it).
- **`packages/core/src/types/DynamicFormItemProps.ts` / `DynamicFormItem.vue` (modified).** Add and forward `insertionOrder?: number`, alongside `globalIndex` (ST-01) and `branchKey`.
- **Public API surface touched:** `FieldMetadata.displayOrder?: 'grouped' | 'added'` (additive, `ComputedPropsFieldType`-excluded) and `ChoiceArrayItemAttributes.insertionOrder?: number` (additive, optional). No `DynamicFormSettings` change ships (Q8 decided against that channel). **Changeset: `minor`**, this story's own entry.
- **Dependencies:** ST-01 (the loop index `globalIndex` must already exist as the render-loop index before this story makes that loop's source list sortable, per the feature architecture: "Depends on A (globalIndex becomes the loop index over the now-sortable render list)").
- **`specs/components.md`:** update the `DynamicFormItemChoice` row (`renderedChoiceOccurrences`, `displayOrder`), the `FieldMetadata` list, and the `ChoiceArrayItemAttributes` type entry once implemented.

## QA plan

This story extends ST-01's `globalIndex` suite and the FEAT-001 `maxOccurs > 1` repeatable-explicit-choice fixtures already established in `DynamicFormItemChoice.logic.test.ts`/`.analytics.test.ts` (the `apiEndpoint`/`crmExport` two-branch fixture). No new fixture component is needed. The same two fixture shapes ST-01 used are reused:

- **Scalar-leaf branches** (`type: 'text'`), matching the existing `maxOccurs > 1` analytics fixture, for tests where occurrence content is not the point. `insertionOrder` never writes into occurrence values (it is purely ephemeral slot-prop data, unlike ST-03's persisted `order`), so unlike ST-03 there is no scalar-branch restriction here: scalar fixtures are safe for every test in this plan, including AC4's "never reaches `values`" assertion in its render-count/structural form.
- **Object branches** (a `children` array, one text child each), named `crmExport`/`apiEndpoint`, used specifically for AC4's `Object.keys()` subset assertion, mirroring ST-01 AC2's pattern for proving no extra key appears inside an occurrence's own object.

This story's dependency on ST-01 means every test below assumes ST-01's `-global-index` testid, `occurrenceGlobalIndex` helper, and `findDynamicFormItemByPath` are already in place and green; this plan does not re-litigate ST-01's own acceptance criteria beyond the specific regression checks called out below.

### Fixture / harness changes

| File | Status | Purpose |
| --- | --- | --- |
| `packages/core/src/examples/TestFormTemplate.vue` | extend | In the `#default-choice-array-item` slot, destructure `insertionOrder` and bind it into a new `${fieldMetadata.path}-insertion-order` testid span, rendered with `v-if="insertionOrder !== undefined"` (unlike `globalIndex`, `insertionOrder` is optional, so its absence is asserted as "the testid does not exist" rather than as a rendered `undefined` string, mirroring ST-01's own precedent for asserting absence of an optional prop). Placed next to the existing `-kind-badge` and (from ST-01) `-global-index` spans. |
| `packages/core/src/components/__tests__/DynamicFormItemChoice.test-helpers.ts` | extend | New `occurrenceInsertionOrder(wrapper, occurrencePath): number \| undefined`, reading the new `-insertion-order` testid (returns `undefined` when the testid does not exist), mirroring `occurrenceBranchKey`/ST-01's `occurrenceGlobalIndex`. New `renderedChoiceOccurrences(wrapper, path): { branchKey: string, index: number }[] \| undefined`, reading the new internal computed off `setupState`, mirroring the existing `activeChoiceOccurrences` reader exactly (same shape, different computed). New `insertionOrdersMap(wrapper, path): Map<string, number> \| undefined`, reading the instance-local `insertionOrders` ref off `setupState`, mirroring `stashedBranchValues`'s "best-effort internal-state reader" rationale, for AC4's stronger "no new store leaks into `values`, and the store itself is instance-local" assertion. New `enableDisplayOrder(metadata, choicePath, mode: 'grouped' \| 'added' = 'added')`, mirroring `enablePreserveOnSwitch`'s recursive metadata-patching shape, centralising the `displayOrder` flag surface so tests do not hardcode the property name. |
| `DynamicFormItem.test-helpers.ts` | no change | Reused for `insertionOrder`'s internal-forwarding-level assertions (AC3, AC8) the same way ST-01 reused it for `globalIndex`: `findDynamicFormItemByPath(...).props('insertionOrder')`. |

A gap surfaced while planning the analytics tests: the architecture calls `renderedChoiceOccurrences` "a new computed", but no `_analytics_renderedChoiceOccurrencesCalculatedCount`-style counter is named anywhere in the story or feature spec, unlike its sibling computeds (`_analytics_activeChoiceOccurrencesCalculatedCount`, `_analytics_occurrencesCalculatedCount`). Without it, "this computed recomputes a bounded number of times per add/remove, not once per unrelated re-render" cannot be pinned the same direct way ST-01 pinned ADR-1's "zero extra logic" claim. **Flagged as a finding below** (Flags for reviewer) rather than silently added to the architecture section (not this phase's section to edit); the plan below proceeds with an indirect (render-count/DOM-identity) proof for the interleave-no-remount claim (AC7) and asks the reviewer to confirm whether a direct counter is worth adding to the architecture before implementation, or whether the indirect proof is sufficient (as it was judged sufficient for ST-01 AC2's comparable negative claim).

### Test files touched

| File | Status | Purpose |
| --- | --- | --- |
| `DynamicFormItemChoice.logic.test.ts` | extend | New `describe('insertionOrder / displayOrder (ST-02)')` block: AC1 (default byte-identical), AC2 (press-order assignment across branches), AC3 (undefined for loaded occurrences), AC4 (never in `values`, instance-local only), AC5 (two-key sort), AC6 (`globalIndex` over the displayed sequence), AC8 (static, `computedProps`-excluded), AC9 (removal renumbers `globalIndex` in interleaved mode), AC10 (`maxOccurs:1` no-op), and the three edge cases. |
| `DynamicFormItemChoice.analytics.test.ts` | extend | New `describe('insertionOrder / displayOrder — render counts (ST-02)')` block: AC7 (interleaved reorder preserves state, no remount), a regression check that `displayOrder` absent reproduces ST-01's exact render-count bounds (the render-list source-list swap from `activeChoiceOccurrences` to `renderedChoiceOccurrences` must not change any existing render-count contract when the sort is a no-op), and the sibling/no-extra-re-render check. |

No change anticipated to `DynamicFormItemChoice.validation.test.ts`: `insertionOrder`/`displayOrder` are pure render-ordering data, never written to `values`, never read by any `xsd_*` rule (`xsd_choiceMinOccurs` reads `effectiveValuesCount`, derived from `activeChoiceOccurrences`/`childValues`, neither of which this story touches). Explicitly noted so the absence of a validation-test addition is not mistaken for a gap, mirroring ST-01's identical note.

### Acceptance criteria → test mapping

> PROPOSED (adversarial review, finding 1): the concrete grouped-order expected arrays below (AC1(c), AC5, AC6) assume `crmExport` is declared before `apiEndpoint`, but the reused `mountExplicitRepeatableChoice` fixture (`DynamicFormItemChoice.logic.test.ts:1589-1606`) declares `apiEndpoint` first, then `crmExport`. `activeChoiceOccurrences` groups strictly by branch declaration order (`DynamicFormItemChoice.vue:281`), so the true grouped order for that fixture is `apiEndpoint`-first, and every literal `toEqual` array that puts `crmExport` before `apiEndpoint` is backwards. Fix: derive all grouped-order literals from the fixture's actual declaration order (apiEndpoint before crmExport). Concretely: AC1(c)'s regression expectation becomes `apiEndpoint[0]=0, apiEndpoint[1]=1, crmExport[0]=2, crmExport[1]=3`; AC5's expected `renderedChoiceOccurrences` becomes `[{ branchKey: 'apiEndpoint', index: 0 }, { branchKey: 'crmExport', index: 0 }, { branchKey: 'crmExport', index: 1 }]` (missing group in apiEndpoint-first grouped order, then the session-added `crmExport[1]`); AC6's grouped-vs-displayed contrast is recomputed against apiEndpoint-first grouping. These literals are coupled to the fixture, so if the branch order is ever flipped (e.g. to match the CRM-first prose in the feature spec), update them and ST-01's identical literals in lockstep. Note: ST-01 (this story's dependency, currently `awaiting-approval`) carries the same backwards literals in its own AC1/edge-case mapping; that is Jeroen's to correct on ST-01, flagged here because ST-02's AC1(c) quotes ST-01's expectation verbatim.

1. **Default is byte-identical to today (flag absent/`'grouped'`).**
   Three tests. (a) `renders byte-identical html whether displayOrder is absent or explicitly 'grouped'`: build two wrappers from the `mountExplicitRepeatableChoice` fixture, one with `displayOrder` absent, one with `enableDisplayOrder(metadata, 'pick', 'grouped')`; run an identical add/add/add/remove sequence (crmExport, apiEndpoint, crmExport, then remove `crmExport[0]`) on both; assert `wrapper.html()` is identical, mirroring FEAT-001's existing "byte-identical html whether `explicitChoiceSelection` is absent or explicitly false" pattern. (b) `renderedChoiceOccurrences equals activeChoiceOccurrences in membership and order` at every step of the same sequence, via the new `renderedChoiceOccurrences`/existing `activeChoiceOccurrences` readers, asserted with `toEqual` after each add/remove. (c) `globalIndex values over the render list are unaffected`: re-run ST-01 AC1's exact fixture and press sequence with `displayOrder` absent, assert `occurrenceGlobalIndex` values are identical to ST-01's own documented expectation (`crmExport[0]=0, crmExport[1]=1, apiEndpoint[0]=2, apiEndpoint[1]=3`), the direct regression guard against a silent list-source swap. `DynamicFormItemChoice.logic.test.ts`.

2. **`insertionOrder` is assigned at add-press time, in press order, across branches.**
   `addChoiceOccurrence('crmExport')`, then `('apiEndpoint')`, then `('crmExport')` again, with `displayOrder` set to `'added'` (assignment happens regardless of `displayOrder`, but reading the value back through the slot exercises the full path). Assert `occurrenceInsertionOrder(wrapper, 'pick.crmExport[0]') === 1`, `occurrenceInsertionOrder(wrapper, 'pick.apiEndpoint[0]') === 2`, `occurrenceInsertionOrder(wrapper, 'pick.crmExport[1]') === 3`, matching press order and independent of grouped (branch-then-index) position. `DynamicFormItemChoice.logic.test.ts`.

3. **`insertionOrder` is `undefined` for occurrences never added this session.**
   Mount `mountExplicitRepeatableChoice` with `initialValues` seeding `pick.crmExport` / `pick.apiEndpoint` occurrences directly (loaded saved data, no `addChoiceOccurrence` call this session). Assert two ways: (a) the `-insertion-order` testid does not exist for either occurrence (the fixture's `v-if`), and (b) `findDynamicFormItemByPath(wrapper, 'pick.crmExport[0]').props('insertionOrder')` is `undefined`, the internal-forwarding-level check, mirroring ST-01 AC4's "stronger than just the prop" dual-check pattern. `DynamicFormItemChoice.logic.test.ts`.

4. **`insertionOrder` never reaches form `values`.**
   Using the object-branch fixture, add several occurrences across both branches (mixing add order and branch), then: (a) `Object.keys(formValues(wrapper).pick.crmExport[0])` etc. for every occurrence contains only the declared child name(s), no `insertionOrder` key, both immediately after adding and after a submit-capture round-trip, mirroring ST-01 AC2's `Object.keys()` proof; (b) `insertionOrdersMap(wrapper, 'pick')` returns a `Map` (confirming the counter lives in an instance-local ref, not a reactive form field), and `Object.keys(formValues(wrapper))` at the top level and recursively contains no `insertionOrder`-shaped key anywhere, a whole-tree negative sweep (recursive key-name scan) rather than only the occurrence-level check, since AC4's wording ("anywhere in it") is stronger than AC2's ST-01 wording ("the occurrence's own values"). `DynamicFormItemChoice.logic.test.ts`.

5. **`displayOrder: 'added'` sorts the render list with a deterministic two-key comparator (AR-3).**
   Mount with `enableDisplayOrder(metadata, 'pick', 'added')` and `initialValues` seeding two pre-existing occurrences (`crmExport[0]`, `apiEndpoint[0]`, both `insertionOrder === undefined`), then call `addChoiceOccurrence('crmExport')` once this session (the new occurrence gets `insertionOrder === 1`). Assert `renderedChoiceOccurrences(wrapper, 'pick')` equals `[{ branchKey: 'crmExport', index: 0 }, { branchKey: 'apiEndpoint', index: 0 }, { branchKey: 'crmExport', index: 1 }]`: the two no-`insertionOrder` occurrences first, in their grouped (`activeChoiceOccurrences`) order, followed by the one session-added occurrence. A second assertion confirms no `NaN`/implementation-defined ordering: repeat the mount/sequence 3 times (fresh wrapper each time, same steps) and assert the resulting order is identical every time (a comparator bug involving `undefined` arithmetic is often non-deterministic only under specific engine sort implementations, so pinning repeatability catches a `NaN`-based comparator that happens to sort stably in the common case). `DynamicFormItemChoice.logic.test.ts`.

6. **`globalIndex` reflects the displayed sequence, not the grouped one, when interleaved.**
   Reuses AC5's exact mixed-set fixture and sequence. Assert, via `occurrenceGlobalIndex`, `pick.crmExport[0].globalIndex === 0`, `pick.apiEndpoint[0].globalIndex === 1`, `pick.crmExport[1].globalIndex === 2`, i.e. the loop-index position within `renderedChoiceOccurrences` (the interleaved sequence), explicitly not the grouped position that `activeChoiceOccurrences` alone would give (`crmExport[0]=0, crmExport[1]=1, apiEndpoint[0]=2` grouped, versus `0, 1, 2` respectively in displayed order here) — the two are deliberately different values for this fixture, making the assertion a real test of "displayed sequence" rather than a coincidental match. `DynamicFormItemChoice.logic.test.ts`.

7. **Interleaved reorder preserves component/field state (no remount).**
   Under the decided AR-3 comparator (stable two-key sort, missing-`insertionOrder` group first in grouped order, numbered group appended after), a newly-added occurrence always appends at the end of the displayed sequence and never demotes an already-rendered occurrence's position, and the missing-`insertionOrder` group's own relative order never changes from an add-press either. So a literal in-place reorder of an *already-rendered* occurrence cannot be produced purely by `addChoiceOccurrence` within a single session; what actually changes `renderedChoiceOccurrences` mid-session is a composition change (an add appends a new entry, a remove drops one and shifts the rest). The test below validates the real, load-bearing engine property (a keyed `v-for` reorder/composition change is remount-free) via a composition-change scenario rather than a literal position swap, since that is the only scenario the decided comparator actually permits: seed `initialValues` with three occurrences (`apiEndpoint[0]`, `crmExport[0]`, `crmExport[1]`, grouped order, all `insertionOrder === undefined`), enter a value into `crmExport[0]`'s field and capture its DOM element reference, render count, and field value; then `addChoiceOccurrence('apiEndpoint')` (new occurrence, `insertionOrder === 1`, appended at the end of the displayed sequence, changing `renderedChoiceOccurrences`'s composition without touching the three existing entries' relative order). Assert `crmExport[0]`'s DOM element is the same reference, its render count is bounded (`+1` at most, the same bound ST-01 established), and its entered field value survived. A second variant repeats the check for a `removeChoiceOccurrence` call instead of an add, since removal is the other composition-changing operation. `DynamicFormItemChoice.analytics.test.ts`, cross-checked with a `.logic.test.ts` companion asserting the entered field value specifically.

   Flagged below (Flags for reviewer): AC7's own wording ("an add-press changes the sort so `renderedChoiceOccurrences`'s order changes") describes a scenario that cannot actually occur for an already-rendered occurrence under the decided AR-3 comparator; the test above proves the equivalent, actually-reachable property (composition change is remount-free) instead. See Flags for reviewer for the proposed rewrite.

8. **`displayOrder` is static, read once at setup.**
   Two tests, mirroring `explicitChoiceSelection is static metadata, not computed` exactly. (a) Runtime: mount with `displayOrder: 'added'` and a `computedProps: [(thisField: any) => { thisField.displayOrder = 'grouped'; }]` on the choice; seed a mixed loaded/added occurrence set (AC5's fixture); assert `renderedChoiceOccurrences` still reflects the interleaved sort captured at setup, unaffected by the mutation. (b) Type-level: `@ts-expect-error displayOrder is excluded from ComputedPropsFieldType` inside a `ComputedPropsFieldOf<Metadata>`-typed function, checked by `pnpm typecheck`/`vue-tsc`, mirroring the existing `explicitChoiceSelection` type-check test line for line. `DynamicFormItemChoice.logic.test.ts`.

9. **Removal renumbers `globalIndex` live in interleaved mode too.**
   `displayOrder: 'added'`, build the AC5 mixed-set style fixture extended to 4 displayed occurrences (2 loaded via `initialValues`, 2 added this session, so the displayed sequence is deterministic and non-trivial), remove the occurrence at displayed position 1, assert the 3 survivors' `globalIndex` re-derive to `0, 1, 2` with no gap, via `occurrenceGlobalIndex`, extending ST-01 AC2's exact assertion style to the sorted list. `DynamicFormItemChoice.logic.test.ts`.

10. **No effect on `maxOccurs: 1` explicit choices.**
    Reuses `mountExplicitSingleChoice` (ST-01 AC4's fixture) with `displayOrder: 'added'` added to the metadata. Assert (a) `wrapper.html()` after a selection is byte-identical to the same fixture with `displayOrder` absent (direct byte-identical regression check, mirroring AC1's own technique and ST-05's "byte-identical" precedent), and (b) neither `-global-index` nor `-insertion-order` testids exist anywhere in the output (the `-choice-array-item` slot never renders on the `maxOccurs:1` path at all, per ST-01 AC4). `DynamicFormItemChoice.logic.test.ts`.

**Edge cases** (same new describe block):
- **All loaded, none added, `displayOrder: 'added'`.** Mount with `initialValues` seeding 3+ occurrences across both branches, no `addChoiceOccurrence` call. Assert `renderedChoiceOccurrences(wrapper, 'pick')` equals `activeChoiceOccurrences(wrapper, 'pick')` exactly (every occurrence falls into the "missing group", so the two-key sort's first key alone determines the result, reproducing grouped order exactly), making the documented fallback concrete and directly testable rather than only inferred from AC5's mixed case.
- **Added, then removed, then a different one added: no counter reuse.** `addChoiceOccurrence('crmExport')` (assert `occurrenceInsertionOrder(..., 'pick.crmExport[0]') === 1`), `removeChoiceOccurrence('crmExport', 0)`, `addChoiceOccurrence('apiEndpoint')`; assert `occurrenceInsertionOrder(..., 'pick.apiEndpoint[0]') === 2`, not `1`, proving the counter does not compact/reuse the removed occurrence's number (no compaction in this ephemeral tier, unlike ST-03's persisted `order`).
- **Nested inside a reindexing ancestor array.** Extends ST-01 AC6's `projectContacts`/`certifications` fixture: add occurrences to `projectContacts[1].certifications` across both branches with `displayOrder: 'added'` set on `certifications`, note each occurrence's `insertionOrder`, remove `projectContacts[0]` (reindexing `projectContacts[1]` to `[0]`), and assert every surviving occurrence's `insertionOrder` (read via the new path prefix) is unchanged by the ancestor reindex, only the path prefix changed, mirroring ST-01 AC6's assertion shape for `globalIndex`.

### States policy (this slice)

Per the feature's states policy, this slice ships no visible output of its own beyond the two new testid spans in the shared test fixture (no docs/template-visual change; the toolbar and visible ordering demo are ST-05). The relevant states to confirm at the data level:

- **Default** (grouped display, `displayOrder` absent, continuous `globalIndex` unaffected): AC1.
- **Empty** (0 occurrences): the `-choice-array-item` loop does not run regardless of `displayOrder`; no `-insertion-order`/render-list-sort-related error. Not separately tested beyond ST-01's existing empty-case coverage remaining green (no new empty-specific behaviour is introduced: an empty `renderedChoiceOccurrences` is trivially `[]` either way).
- **Error** (`xsd_choiceMinOccurs`): out of scope, unaffected by ordering (confirmed by the "No change anticipated to `.validation.test.ts`" note above); guarded by the existing suite remaining green.
- **Disabled** (`maxOccurs: 0` / budget exhausted): unaffected; a disabled choice has no active occurrences to sort, collapsing to Empty. No dedicated test beyond existing coverage remaining green.
- **Grouped ordering** (default): AC1, edge case 1.
- **Interleaved ordering** (`displayOrder: 'added'`): AC2, AC5, AC6, AC7, AC9, edge cases 2-3. This is the states-policy "Ordering" bullet's ephemeral half (`#ordering-interleaved`, `#reload-behavior`'s ephemeral side); the persisted half (`#ordering-preserve`) is ST-03's scope.
- **Removal** (live-derived renumbering, `#numbering-removal`'s pattern extended to the sorted list): AC9.

### Reactivity / `*.analytics.test.ts` plan

This slice modifies `DynamicFormItemChoice.vue` (new `insertionOrders` ref, `insertionCounter` plain variable, new `renderedChoiceOccurrences` computed, the `-choice-array-item` `v-for`'s source-list swap) and `DynamicFormItem.vue`/`DynamicFormItemProps.ts` (new `insertionOrder` prop forwarded alongside `globalIndex`). Extend `DynamicFormItemChoice.analytics.test.ts`:

1. **`displayOrder` absent reproduces ST-01's exact render-count bounds.** Re-run ST-01's own analytics tests ("adding an occurrence mounts exactly one new `DynamicFormItem`...", "removing occurrence 0 of two does not remount the surviving occurrence...") verbatim against the post-ST-02 codebase, asserting the same bounds (render count `+1` at most, same DOM node identity), to catch a regression where switching the `v-for`'s source list from `activeChoiceOccurrences` to `renderedChoiceOccurrences` (a no-op sort when `displayOrder` is falsy) introduces an extra reactive dependency and a spurious re-render.
2. **Interleaved reorder/composition change does not remount a surviving occurrence (AC7).** As detailed in the AC7 mapping above: capture DOM element identity, render count, and entered field value for an occurrence immediately before and after an add or remove that changes `renderedChoiceOccurrences`'s composition, assert identity preserved, render count bounded (`+1` at most, same bound ST-01 established for the unsorted case), and the entered value intact.
3. **No sibling re-render.** Reuses the existing `sibling` field pattern: assigning `insertionOrder`/computing `renderedChoiceOccurrences` on add/remove, with `displayOrder: 'added'`, does not change `renderCount(wrapper, 'sibling')`.
4. **`_analytics_activeChoiceOccurrencesCalculatedCount`/`_analytics_occurrencesCalculatedCount` are unchanged by this story.** Same rationale as ST-01's equivalent test: this story adds a new computed (`renderedChoiceOccurrences`) but does not change how or how often the pre-existing computeds recompute; assert their existing recompute-count contracts (one recompute per add/remove call) stay byte-identical before and after this story's change.
5. **`_analytics_renderedChoiceOccurrencesCalculatedCount` recomputes a bounded number of times.** PROPOSED (adversarial review, finding 3): with the counter added per the Architecture-reference edit above, this test is unconditional (no longer a placeholder). Assert the counter increases by a bounded amount per `addChoiceOccurrence`/`removeChoiceOccurrence` call (mirroring test 4's siblings) and does not increase on an unrelated sibling-field change, the same direct proof ST-01 used for its own "zero extra logic" claim, but now covering this story's actual new computed rather than only inferring its discipline indirectly.

### Coverage

Every new runtime line this story introduces is covered by the AC-mapped tests above: the `displayOrder` static capture (exercised by AC1's absent/`'grouped'` paths and AC5-AC9's `'added'` path), the `insertionOrders` ref + `insertionCounter` assignment in `addChoiceOccurrence` (AC2, AC3, edge case 2), the `renderedChoiceOccurrences` computed's both branches (identity passthrough: AC1; two-key sort: AC5, AC6, AC9, edge cases 1 and 3), the `-choice-array-item` `v-for` source-list swap (every occurrence in every test above renders through it), and the `insertionOrder` prop declaration/forwarding in `DynamicFormItemProps.ts`/`DynamicFormItem.vue` (AC2/AC3's defined/`undefined` paths, AC10's absence check). The `FieldMetadata.displayOrder` addition and its `ComputedPropsFieldType` exclusion are covered by AC8's runtime and type-level tests respectively. The `DynamicFormTemplate.vue` `ChoiceArrayItemAttributes.insertionOrder` addition is type-only, exercised at the type-check level by `pnpm typecheck` (the fixture's slot destructures `insertionOrder`) and behaviourally by every testid-based assertion above. Run `pnpm -r ci:test:coverage` after implementation and confirm no drop against baseline; no code path in this story is knowingly left uncovered, aside from the placeholder analytics test (item 5 above) which depends on whether the reviewer/architecture adds the suggested counter.

### Time sensitivity

Not applicable. No date/time-dependent logic is introduced by this story.

### Regression risk

- **`DynamicFormItemChoice.vue`'s `-choice-array-item` `v-for` source list** — the highest-risk change in this story: swapping from `activeChoiceOccurrences` to `renderedChoiceOccurrences` must be byte-identical in every observable respect when `displayOrder` is falsy. Guarded by AC1's three-pronged test (byte-identical HTML, list-equality, `globalIndex` regression against ST-01's own fixture) plus the full existing ST-01 and FEAT-001 `maxOccurs > 1` describe blocks in `.logic.test.ts`/`.validation.test.ts`/`.analytics.test.ts` remaining green, unmodified.
- **`activeChoiceOccurrences` itself** — explicitly untouched per the feature's hard constraint; no test in this plan reads or mutates it beyond the existing assertions, and `renderedChoiceOccurrences`'s own correctness (AC5/AC6/AC9) is defined directly in terms of it, so a regression there surfaces in both this story's and the existing tests.
- **`DynamicFormItem.vue`'s leaf-template forwarding block** — a second optional prop (`insertionOrder`) is added alongside ST-01's `globalIndex`; must not change either prop's value or timing. Guarded by the full existing `DynamicFormItem*`/`DynamicFormItemArray*`/`DynamicFormItemChoice*` suites remaining green, plus ST-01's own `globalIndex` tests re-run unmodified as part of this story's AC1 regression checks.
- **`TestFormTemplate.vue` fixture** — the one addition (a conditionally-rendered `-insertion-order` testid span) is strictly additive next to the existing `-kind-badge`/`-global-index` spans; re-run the full existing suite (this fixture backs nearly every `DynamicFormItem*` test) to confirm no unintended markup/spacing regression.
- **Downstream stories (ST-03, ST-05)** — both depend on this story (ST-03 directly reuses `renderedChoiceOccurrences`'s sort infra with `order` as the key; ST-05 demonstrates both tiers). A regression here blocks the rest of the feature.
- **ST-01's own tests** — must remain green, unmodified, as the primary proof that this story's changes to the same `v-for`/prop-forwarding surface did not alter ST-01's contract.

### Manual verification checklist

None required. This is an engine-only story with no docs/template-visual slice (the toolbar control and visible ordering demo are ST-05's scope), and every acceptance criterion and edge case is exercised through the `TestForm`/`TestFormTemplate` component-test harness with `@vue/test-utils`.

### Flags for reviewer

- **AC7's scenario appears unreachable as literally worded under the decided AR-3 comparator.** AR-3's stable two-key sort means a newly-added occurrence always appends at the end of the numbered group and never demotes an already-rendered occurrence's position; a loaded (no-`insertionOrder`) occurrence's relative order among other no-`insertionOrder` occurrences also never changes from an add-press. So "an add-press changes the sort so `renderedChoiceOccurrences`'s order changes" for an *already-rendered* occurrence cannot actually happen within a single session purely from adding; a composition change (add or remove) is what actually happens, and Vue's keyed diff is what makes it remount-free either way. The test plan above still validates the real, load-bearing engine property via a composition-change scenario, but flagging the AC wording as a rewrite candidate: "when `renderedChoiceOccurrences`'s composition changes" instead of "order changes", or supply a concrete scenario where a genuine reorder is possible (none was found; if one is intended, e.g. via a future feature interaction, it should be named).
- **No `_analytics_renderedChoiceOccurrencesCalculatedCount`-style counter exists for the new computed.** Every prior computed this component gained (`activeChoiceOccurrences`, `occurrences`) got a matching `_analytics_*CalculatedCount` counter for direct render-count-discipline testing. This story's own architecture section does not name one for `renderedChoiceOccurrences`. The plan above compensates with an indirect (DOM-identity/render-count-bound) proof for AC7, the same class of proof ST-01's reviewer accepted for AC2's comparable negative claim, but a direct counter would be a stronger, cheaper-to-maintain test. Flagging for the reviewer/architect to confirm the indirect proof is acceptable, consistent with the ST-01 precedent, or whether the counter should be added to the architecture before implementation.
- **AC4's "never reaches form `values`" is tested as a whole-tree recursive key-name sweep**, stronger than ST-01 AC2's occurrence-scoped `Object.keys()` check, because AC4's own wording ("anywhere in it") is broader than AC2's ("the occurrence's own values"). Flagging the scope difference so the reviewer can confirm a recursive sweep is the right strength here rather than over-testing; the story introduces no named ref that would let `values` acquire an `insertionOrder` key anywhere else in the tree, so this is a robustness margin, not a response to a specific suspected leak.
- **The `-insertion-order` testid is conditionally rendered (`v-if`), unlike ST-01's always-rendered `-global-index` testid.** This is a real behavioural difference (optional vs. required slot prop) reflected faithfully in the fixture, not an inconsistency; flagging so it is not mistaken for a fixture bug during review.

## Adversarial review

Filled by adversarial-reviewer (2026-09-19). Model: claude-opus-4-8. STORY mode (lite): blockers only.

Verification done against the actual code, not just the prose: `DynamicFormItemChoice.vue` (the `-choice-array-item` `v-for` at line 735-752, `activeChoiceOccurrences` at 276-305, `addChoiceOccurrence`'s `maxOccurs > 1` branch at 514-521, `occurrenceKey`/`rawOccurrenceKey` at 623-635), `FieldMetadata.ts` (`ComputedPropsFieldType` `Omit` list at 227-262, confirming `explicitChoiceSelection`/`preserveOnSwitch` are excluded and `displayOrder` slots in the same way), the reused test fixtures (`mountExplicitRepeatableChoice` at `DynamicFormItemChoice.logic.test.ts:1589`, `mountExplicitSingleChoice`, `mountExplicitRepeatableChoiceWithSibling`), and the test-helpers (`activeChoiceOccurrences`/`stashedBranchValues`/`enablePreserveOnSwitch`/`occurrenceBranchKey` readers all exist).

**Confirmed correct (the load-bearing claims hold):**
- **The add-time assignment is mechanically achievable.** `addChoiceOccurrence`'s `maxOccurs > 1` branch (line 519) does `push(null); return;`. Inserting `insertionOrders.value.set(occurrenceKey({ branchKey, index: L }), ++insertionCounter)` between them works: after `push`, `fields.value[L]` exists (L = branch length before push) and carries a stable vee-validate `.key`, so `rawOccurrenceKey`/`occurrenceKey` resolve. `ref(new Map())` is a reactive Map in Vue 3, so `.set()` triggers `renderedChoiceOccurrences` to recompute and the new occurrence's `insertionOrder` shows at first render (no stale flash).
- **Instance-local isolation and reindex stability hold.** `occurrenceKey` is `branchKey:rawKey`, both stable across ancestor reindex (rawKey is vee-validate's field-array key, not DOM position), so the edge case "nested inside a reindexing array" behaves as claimed, matching ST-01 AC6 for `globalIndex`. `insertionOrder` never enters `values` (it lives in an instance-local ref), so AC4 is sound.
- **The falsy-`displayOrder` fast path avoids a reactive dependency.** `displayOrder` is a static const captured at setup, so `renderedChoiceOccurrences` early-returns `activeChoiceOccurrences` without reading the `insertionOrders` map, so the source-list swap introduces no extra dependency when the sort is a no-op. AC1's byte-identical claim and the analytics regression test guard this.
- **Consistency with the approved feature and its DECIDED entries.** `activeChoiceOccurrences` untouched (finding 5 / hard constraint); `displayOrder` added to the `ComputedPropsFieldType` `Omit` list like its static siblings; deterministic two-key sort (AR-3); no `DynamicFormSettings` change (Q8); `insertionOrder` only on `ChoiceArrayItemAttributes`, never `ChoiceOccurrence` (AR-5); changeset `minor`. Dependency on ST-01 explicit. No silent local override of the feature design/architecture.

### Findings

1. **(should-fix) The QA plan's concrete grouped-order expected arrays are backwards relative to the reused fixture.** AC1(c), AC5, and AC6 assume `crmExport` is declared before `apiEndpoint`, but `mountExplicitRepeatableChoice` (`DynamicFormItemChoice.logic.test.ts:1599-1600`) declares `apiEndpoint` first. `activeChoiceOccurrences` groups strictly by branch declaration order (`DynamicFormItemChoice.vue:281`), so grouped order is apiEndpoint-first and every literal `toEqual` array that puts `crmExport` before `apiEndpoint` would fail against a correct engine. The AC prose (grouped = branch-then-index position) is correct and testable; only the illustrative literals are wrong, so this is a mapping defect, not an untestable AC. The feature spec's CRM-first prose is likely the source of the mental slip. Routed as PROPOSED (adversarial review, finding 1) into the "Acceptance criteria → test mapping" preamble, correcting the literals to the fixture's actual apiEndpoint-first order. ST-01 (the dependency, `awaiting-approval`) carries the identical backwards literals and ST-02's AC1(c) quotes them verbatim; that is Jeroen's to correct on ST-01, noted for coordination but out of this phase's edit scope.

2. **(should-fix) AC7's scenario is unreachable as literally worded under the decided AR-3 comparator (QA flag 1).** The qa-planner's flag is correct: AR-3's append-only stable comparator means an add never demotes an already-rendered occurrence's position, so "an add-press changes the sort so `renderedChoiceOccurrences`'s order changes" for an already-rendered occurrence cannot happen within a session (every add is an append to the displayed sequence; verified against the comparator and against the pure-session interleave case). The load-bearing property AC7 exists to protect (keyed `v-for` mutation is remount-free) is real and is reachable via a composition change (add appends, remove drops and shifts), which the QA plan already tests. Routed as PROPOSED (adversarial review, finding 2) into AC7, rewording the When clause to "an add or remove changes `renderedChoiceOccurrences`'s composition". Ruling: accept the qa-planner's proposed rewrite; no genuine-reorder scenario exists and none is intended.

3. **(should-fix) No `_analytics_renderedChoiceOccurrencesCalculatedCount` counter for the new computed (QA flag 2).** Every prior computed this component gained (`activeChoiceOccurrences`, `occurrences`) has a matching `_analytics_*CalculatedCount` plain-variable counter (`DynamicFormItemChoice.vue:50-51,181,277`) for direct render-discipline testing; this story's new `renderedChoiceOccurrences` computed has none named. Ruling: the ST-01 precedent (indirect DOM-identity proof accepted) does not transfer cleanly, because ST-01 added zero new computeds ("zero extra logic") whereas ST-02 adds a genuinely new reactive computed, exactly the class of thing the sibling counters exist to guard against a recompute storm. The direct counter is the stronger, cheaper-to-maintain guard and matches the component's established pattern. Routed as PROPOSED (adversarial review, finding 3) into the Architecture reference (add the counter) and `.analytics.test.ts` plan item 5 (make the direct test unconditional rather than a placeholder).

4. **(nit) AC4's whole-tree recursive key-name sweep (QA flag 3) is the right strength, not over-testing.** AC4's wording ("anywhere in it") is genuinely broader than ST-01 AC2's occurrence-scoped check, and the story introduces no named ref that could let `values` acquire an `insertionOrder` key elsewhere, so the recursive sweep is a cheap robustness margin. No change needed.

5. **(nit) The conditionally-rendered (`v-if`) `-insertion-order` testid (QA flag 4) correctly reflects an optional slot prop.** Asserting absence as "the testid does not exist" (rather than a rendered `undefined` string) is the faithful modelling of an optional prop and mirrors ST-01's precedent for absence. No change needed; not a fixture bug.

6. **(nit) AC6's "versus 0, 1, 2 respectively in displayed order here" parenthetical is confusingly ordered.** The per-occurrence assertion values in the sentence are correct, but the trailing "respectively" list does not line up with the occurrence order it follows. Minor wording; will be swept up when finding 1's literals are corrected against the fixture. No independent action.

### Status rationale

No blockers, no Open questions of its own. Every acceptance criterion is testable (the AC prose is sound; the three should-fixes are a mapping-literal error, an unreachable-as-worded When clause, and a missing render-discipline counter, all mechanical), each maps to a concrete assertion against helpers and fixtures that exist, the add-time assignment and reactive-Map mechanics check out against the code, and nothing contradicts the approved feature's DECIDED entries (Q8 static per-choice `displayOrder`, AR-3 two-key sort, AR-5 no `ChoiceOccurrence.insertionOrder`) or its slicing. All three should-fixes are routed as `PROPOSED (adversarial review)` edits, so none independently forces discussion. Per `specs/README.md` this lands in `awaiting-approval`. The one item that spans stories (finding 1's identical backwards literals in ST-01) is flagged for Jeroen but does not block ST-02.
