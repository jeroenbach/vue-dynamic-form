{
  "name": "vue-dynamic-form-workspace",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "cd playgrounds/storybook && pnpm install --ignore-workspace",
    "dev": "cd packages/core && pnpm build:watch & cd playgrounds/storybook && pnpm dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "ci": "pnpm -r ci",
    "ci:lint": "pnpm -r ci:lint",
    "ci:test": "pnpm -r ci:test",
    "ci:typecheck": "pnpm -r ci:typecheck",
    "ci:build": "pnpm -r ci:build",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:dev",
    "@changesets/cli": "catalog:tooling"
  }
}
